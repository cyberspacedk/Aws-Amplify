import React from 'react'

const DeletePost = ({id}) => {

  const deletePost = (id) => {

  };

  return (
    <button onClick={deletePost}>
      Delete Post
    </button>
  )
}

export default DeletePost;